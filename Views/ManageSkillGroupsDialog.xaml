<ui:FluentWindow
    x:Class="SkillManager.Views.ManageSkillGroupsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="管理技能分组"
    Width="400"
    Height="450"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  标题栏  -->
        <ui:TitleBar Grid.Row="0" Title="管理技能分组" />

        <!--  主内容区  -->
        <Grid Grid.Row="1" Margin="24,12,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  技能信息  -->
            <ui:Card Grid.Row="0" Margin="0,0,0,16" Padding="12">
                <StackPanel>
                    <TextBlock
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{Binding SkillName}" />
                    <TextBlock
                        Margin="0,4,0,0"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="选择要加入的分组（可多选）" />
                </StackPanel>
            </ui:Card>

            <!--  分组列表  -->
            <ui:Card Grid.Row="1" Padding="8">
                <Grid>
                    <ui:ListView
                        x:Name="GroupsListView"
                        ItemsSource="{Binding Groups}"
                        SelectionMode="Multiple"
                        VirtualizingPanel.IsVirtualizing="True">
                        <ui:ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="8,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <CheckBox
                                        Grid.Column="0"
                                        Margin="0,0,12,0"
                                        VerticalAlignment="Center"
                                        IsChecked="{Binding IsSelected, Mode=TwoWay}" />

                                    <TextBlock
                                        Grid.Column="1"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Text="{Binding Name}" />

                                    <TextBlock
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                                        <Run Text="{Binding SkillNames.Count, Mode=OneWay}" />
                                        <Run Text="个技能" />
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                    </ui:ListView>

                    <!--  空状态  -->
                    <StackPanel
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="{Binding HasNoGroups, Converter={StaticResource BoolToVisibilityConverter}}">
                        <ui:SymbolIcon
                            FontSize="48"
                            Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                            Symbol="FolderOpen24" />
                        <TextBlock
                            Margin="0,8,0,0"
                            HorizontalAlignment="Center"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="暂无分组" />
                        <TextBlock
                            Margin="0,4,0,0"
                            HorizontalAlignment="Center"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                            Text="请先创建分组" />
                    </StackPanel>
                </Grid>
            </ui:Card>
        </Grid>

        <!--  底部按钮  -->
        <Grid Grid.Row="2" Margin="24,16,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:Button
                Grid.Column="1"
                Margin="0,0,8,0"
                Content="取消"
                Click="CancelButton_Click" />

            <ui:Button
                Grid.Column="2"
                Appearance="Primary"
                Content="保存"
                Icon="{ui:SymbolIcon Checkmark24}"
                Click="SaveButton_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
