<Page
    x:Class="SkillManager.Views.ScanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:SkillManager.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:SkillManager.ViewModels"
    Title="扫描导入"
    d:DataContext="{d:DesignInstance vm:ScanViewModel}"
    d:DesignHeight="600"
    d:DesignWidth="900"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  标题区  -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock
                FontSize="28"
                FontWeight="SemiBold"
                Text="扫描导入" />
            <TextBlock
                Margin="0,4,0,0"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="扫描文件系统中的技能文件夹（包含 SKILL.md 的目录）" />
        </StackPanel>

        <!--  扫描控制区  -->
        <ui:Card Grid.Row="1" Margin="0,0,0,16" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  路径选择  -->
                    <Grid Grid.Column="0" Margin="0,0,16,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ui:TextBox
                            Grid.Column="0"
                            Icon="{ui:SymbolIcon Folder24}"
                            IsReadOnly="True"
                            PlaceholderText="选择要扫描的目录..."
                            Text="{Binding ScanPath}" />
                        <ui:Button
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            Command="{Binding BrowsePathCommand}"
                            Content="浏览"
                            Icon="{ui:SymbolIcon FolderOpen24}" />
                    </Grid>

                    <!--  扫描按钮  -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <ui:Button
                            Margin="0,0,8,0"
                            Appearance="Primary"
                            Command="{Binding ScanCommand}"
                            Content="扫描目录"
                            Icon="{ui:SymbolIcon Search24}"
                            IsEnabled="{Binding IsScanning, Converter={StaticResource BoolToInverseConverter}}" />
                        <ui:Button
                            Margin="0,0,8,0"
                            Appearance="Info"
                            Command="{Binding ScanGlobalCommand}"
                            Content="全局扫描"
                            Icon="{ui:SymbolIcon Globe24}"
                            IsEnabled="{Binding IsScanning, Converter={StaticResource BoolToInverseConverter}}"
                            ToolTip="扫描所有本地驱动器" />
                        <ui:Button
                            Appearance="Caution"
                            Command="{Binding CancelScanCommand}"
                            Content="取消"
                            Icon="{ui:SymbolIcon Dismiss24}"
                            Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>
                </Grid>

                <!--  进度信息  -->
                <StackPanel
                    Grid.Row="1"
                    Margin="0,12,0,0"
                    Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ui:ProgressRing IsIndeterminate="True" />
                </StackPanel>
            </Grid>
        </ui:Card>

        <!--  扫描结果列表  -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  结果标题栏  -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="SemiBold">
                    <Run Text="发现的技能 (" />
                    <Run Text="{Binding FoundSkills.Count, Mode=OneWay}" />
                    <Run Text=")" />
                </TextBlock>

                <ui:Button
                    Grid.Column="1"
                    Appearance="Success"
                    Command="{Binding ImportAllCommand}"
                    Content="全部导入"
                    Icon="{ui:SymbolIcon ArrowDownload24}"
                    IsEnabled="{Binding FoundSkills.Count, Converter={StaticResource CountToBoolConverter}}" />
            </Grid>

            <!--  结果列表  -->
            <ui:ListView Grid.Row="1" ItemsSource="{Binding FoundSkills}">
                <ui:ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:SkillFolder}">
                        <ui:Card Margin="0,4" Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!--  图标  -->
                                <Border
                                    Grid.Column="0"
                                    Width="48"
                                    Height="48"
                                    Margin="0,0,16,0"
                                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                                    CornerRadius="8">
                                    <ui:SymbolIcon
                                        FontSize="24"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Symbol="Code24" />
                                </Border>

                                <!--  信息  -->
                                <StackPanel
                                    Grid.Column="1"
                                    VerticalAlignment="Center">
                                    <TextBlock
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Text="{Binding Name}" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Text="{Binding Description}"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock
                                        Margin="0,4,0,0"
                                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                        Text="{Binding FullPath}"
                                        TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <!--  操作按钮  -->
                                <StackPanel
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <ui:Button
                                        Margin="0,0,8,0"
                                        Appearance="Secondary"
                                        Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=ui:ListView}}"
                                        CommandParameter="{Binding}"
                                        Icon="{ui:SymbolIcon FolderOpen24}"
                                        ToolTip="打开源文件夹" />
                                    <ui:Button
                                        Appearance="Primary"
                                        Command="{Binding DataContext.ImportSelectedCommand, RelativeSource={RelativeSource AncestorType=ui:ListView}}"
                                        CommandParameter="{Binding}"
                                        Content="导入"
                                        Icon="{ui:SymbolIcon ArrowDownload24}" />
                                </StackPanel>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>

            <!--  空状态  -->
            <ui:Card
                Grid.Row="1"
                Padding="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding FoundSkills.Count, Converter={StaticResource CountToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center">
                    <ui:SymbolIcon
                        FontSize="64"
                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                        Symbol="Search24" />
                    <TextBlock
                        Margin="0,16,0,0"
                        HorizontalAlignment="Center"
                        FontSize="18"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="暂无扫描结果" />
                    <TextBlock
                        Margin="0,8,0,0"
                        HorizontalAlignment="Center"
                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                        Text="选择目录或进行全局扫描" />
                </StackPanel>
            </ui:Card>
        </Grid>

        <!--  状态栏  -->
        <ui:Card Grid.Row="3" Margin="0,16,0,0" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="{Binding StatusMessage}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                    Text="{Binding ScanStats}" />
            </Grid>
        </ui:Card>
    </Grid>
</Page>
