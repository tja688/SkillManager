<Page
    x:Class="SkillManager.Views.ProjectListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:SkillManager.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:SkillManager.ViewModels"
    Title="项目"
    d:DataContext="{d:DesignInstance vm:ProjectListViewModel}"
    d:DesignHeight="600"
    d:DesignWidth="900"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  标题栏  -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <ui:SymbolIcon
                    Margin="0,0,12,0"
                    FontSize="28"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Symbol="Folder24" />
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="项目" />
            </StackPanel>

            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,8,0"
                    Appearance="Primary"
                    Click="AddProject_Click"
                    Icon="{ui:SymbolIcon Add24}">
                    添加项目
                </ui:Button>
                <ui:Button
                    Command="{Binding RefreshProjectsCommand}"
                    Icon="{ui:SymbolIcon ArrowSync24}"
                    ToolTip="刷新" />
            </StackPanel>
        </Grid>

        <!--  项目列表  -->
        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Projects}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Project}">
                        <ui:Card
                            Width="280"
                            Margin="0,0,16,16"
                            Padding="16"
                            Cursor="Hand"
                            MouseLeftButtonUp="ProjectCard_Click">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!--  项目名称  -->
                                <TextBlock
                                    Grid.Row="0"
                                    FontSize="16"
                                    FontWeight="SemiBold"
                                    Text="{Binding Name}"
                                    TextTrimming="CharacterEllipsis" />

                                <!--  项目路径  -->
                                <TextBlock
                                    Grid.Row="1"
                                    Margin="0,4,0,0"
                                    FontSize="12"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Path}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTip="{Binding Path}" />

                                <!--  统计信息  -->
                                <StackPanel
                                    Grid.Row="2"
                                    Margin="0,12,0,0"
                                    Orientation="Horizontal">
                                    <StackPanel Margin="0,0,16,0" Orientation="Horizontal">
                                        <ui:SymbolIcon
                                            Margin="0,0,4,0"
                                            FontSize="14"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Symbol="Code24" />
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="13"
                                            Text="{Binding TotalSkillCount, StringFormat={}{0} 个技能}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon
                                            Margin="0,0,4,0"
                                            FontSize="14"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Symbol="Calendar24" />
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontSize="13"
                                            Text="{Binding CreatedTime, StringFormat={}{0:yyyy/MM/dd}}" />
                                    </StackPanel>
                                </StackPanel>

                                <!--  操作按钮  -->
                                <StackPanel
                                    Grid.Row="3"
                                    Margin="0,12,0,0"
                                    HorizontalAlignment="Right"
                                    Orientation="Horizontal">
                                    <ui:Button
                                        Margin="0,0,4,0"
                                        Padding="8,4"
                                        Command="{Binding DataContext.OpenProjectFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Icon="{ui:SymbolIcon FolderOpen24}"
                                        ToolTip="打开文件夹" />
                                    <ui:Button
                                        Padding="8,4"
                                        Command="{Binding DataContext.DeleteProjectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"
                                        Icon="{ui:SymbolIcon Delete24}"
                                        ToolTip="删除项目" />
                                </StackPanel>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!--  空状态提示  -->
        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="{Binding Projects.Count, Converter={StaticResource CountToVisibilityConverter}}">
            <ui:SymbolIcon
                FontSize="64"
                Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                Symbol="FolderOpen24" />
            <TextBlock
                Margin="0,16,0,0"
                HorizontalAlignment="Center"
                FontSize="16"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="暂无项目" />
            <TextBlock
                Margin="0,8,0,0"
                HorizontalAlignment="Center"
                FontSize="13"
                Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                Text="点击上方「添加项目」按钮创建新项目" />
        </StackPanel>

        <!--  状态栏  -->
        <Border
            Grid.Row="2"
            Margin="0,16,0,0"
            Padding="12,8"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            CornerRadius="4">
            <TextBlock
                FontSize="13"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{Binding StatusMessage}" />
        </Border>
    </Grid>
</Page>
