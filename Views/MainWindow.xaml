<ui:FluentWindow
    x:Class="SkillManager.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SkillManager.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:SkillManager.ViewModels"
    Title="Skill Manager"
    Width="1200"
    Height="800"
    MinWidth="900"
    MinHeight="600"
    d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  标题栏  -->
        <ui:TitleBar
            Title="Skill Manager"
            Grid.Row="0">
            <ui:TitleBar.Header>
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon
                        Margin="8,0"
                        FontSize="20"
                        Symbol="Code24" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="Skill Manager" />
                </StackPanel>
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <!--  主内容区  -->
        <ui:NavigationView
            x:Name="NavigationView"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleVisible="True"
            OpenPaneLength="280"
            PaneDisplayMode="Left"
            SelectionChanged="NavigationView_SelectionChanged">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                    x:Name="LibraryNavItem"
                    Content="技能库"
                    Icon="{ui:SymbolIcon Library24}"
                    Tag=""
                    MouseLeftButtonUp="LibraryNavItem_MouseLeftButtonUp"
                    TargetPageType="{x:Type local:AllSkillsPage}" />
                <ui:NavigationViewItem
                    Content="扫描导入"
                    Icon="{ui:SymbolIcon Search24}"
                    TargetPageType="{x:Type local:ScanPage}" />
                <ui:NavigationViewItem
                    Content="项目"
                    Icon="{ui:SymbolIcon Folder24}"
                    TargetPageType="{x:Type local:ProjectListPage}" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem
                    Content="设置"
                    Icon="{ui:SymbolIcon Settings24}"
                    TargetPageType="{x:Type local:SettingsPage}" />
            </ui:NavigationView.FooterMenuItems>

        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
